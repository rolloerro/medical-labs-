🧪 MED-LABS: Laboratory Request Gateway
Сервис приёма и валидации лабораторных направлений (JSON)

MED-LABS — это лёгкий и быстрый Node.js-сервис, который принимает лабораторные направления от клиник, мобильных приложений и медицинских ботов.
Он проверяет структуру данных (JSON-схему), валидирует параметры анализа и сохраняет корректные заявки в локальное хранилище или MinIO.

📌 Зачем это нужно?

Медицинским системам нужно безопасно и одинаково передавать направления:
анализы крови
биохимию
ПСА
цитологию
ПЦР
и любые кастомные панели

MED-LABS решает проблему хаотичных структур, неправильных JSON и разных форматов — он приводит всё к единой схеме.

🚀 Возможности
✔ Приём направлений

POST /lab/request принимает JSON с данными пациента и анализов.

✔ Проверка схемы через AJV

Сервис проверяет:
структуру JSON
обязательные поля
форматы дат
значения анализов
вложенные объекты
Ошибки чёткие и читаемые.

✔ Сохранение валидных заявок
Все корректные запросы складываются в:
./requests
(или MinIO, если подключено)
✔ Лёгкий бэкенд
Fastify + модульная структура — быстро, просто, надёжно.
✔ Docker-ready
Сервис можно запустить в контейнере одной командой.
📁 Структура проекта
/src
  /routes
    lab.js              — маршруты API
  /schemas
    labRequestSchema.js — JSON-схема валидации
  app.js                — основной сервер

/requests               — сохранённые заявки
.env.example            — пример конфигурации
Dockerfile
🔧 Быстрый старт

Установка:

npm install

Запуск в разработке:

npm run dev

Сервис слушает:
http://localhost:3005

📤 Пример запроса
POST /lab/request
Content-Type: application/json
{
  "patient": {
    "id": "PAT-2025-001",
    "name": "Иванов Иван",
    "dob": "1988-04-12",
    "gender": "male"
  },
  "tests": [
    { "type": "cbc" },
    { "type": "psa", "value": 1.22 }
  ]
}
Пример ответа:
{
  "status": "OK",
  "saved": "request_2025-12-05_17-30.json"
}
🐳 Запуск через Docker

Собрать контейнер:

docker build -t med-labs .

Запустить:

docker run -p 3005:3005 med-labs
⚖ Лицензия
MIT — свободно, можно использовать где угодно.

👨‍⚕️ Автор
Проект создан для тренировки медицинских интеграций и построения экосистемы мед-ботов.

